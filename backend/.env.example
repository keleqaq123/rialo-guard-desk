RIALO_RPC_URL=https://api.devnet.rialo.xyz
SERVER_ADDR=127.0.0.1:8080

# Optional: label shown in UI.
RIALO_NETWORK_NAME=Rialo Devnet

# Optional: service wallet pubkey for display only.
RIALO_SIGNER_PUBKEY=9h1HyLCW5dZnBVap8C5egQ9Z6pHyjsh5MNy83iPqqRuq

# Optional: 32-byte hex secret-sharing pubkey used by rialo_cdk::encrypt_secret.
# If omitted, the API will still work and return a mock encrypted preview for UI testing.
RIALO_SECRET_SHARING_PUBKEY_HEX=
